# This repository was created from the slate template
# For more information, visit: https://github.com/fudohermes/slate

This is a template repository. To use this as a starting point for your project:

1. Click "Use this template" button on GitHub
2. Clone the new repository
3. Run `nix develop` to enter development environment
4. Run `npm install && npm run watch` to start development
5. Update repository.yaml with your project information

## Quick commands

```bash
nix develop              # Enter development environment
npm install             # Install dependencies
npm run watch           # Start development server with hot reload
npm run build           # Production build
npm run docker:build    # Build Docker image
kubectl apply -f k8s/   # Deploy to Kubernetes
```

Happy coding! 🚀
